<div class="container-fluid my-5">
    <div class="row">
        <div class="col-lg-6">
            <div class="d-flex justify-content-center align-items-center flex-column">
                <h4 class="text-center mb-3"><b>{{ this.fullname }}</b></h4>
                <p class="text-center"><b>Username:</b> {{ this.username }}</p>
                <p class="text-center"><b>Mail:</b> {{ this.email }}</p>
                <p class="text-center"><b>Role:</b> {{ this.role }}</p>
            </div>
        </div>
        <div class="col-lg-6 d-flex align-items-center justify-content-center">
            <div class="d-flex align-items-center">
                <button class="btn btn-warning btn-lg carrito mx-2">Carrito</button>
                <a href="/session/premium/{{ this.id }}" class="btn btn-info btn-lg mx-2">Premium</a>
                <button class="btn btn-info m-2" onclick="location.href='/productos'"> Volver al Inicio</button>
                <a href="/session/logout" class="btn btn-danger btn-lg mx-2">Cerrar sesión</a>
            </div>
        </div>
    </div>
</div>

<script src="/js/products.js"></script>
